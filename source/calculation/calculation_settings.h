#ifndef CALCULATION_SETTINGS_H
#define CALCULATION_SETTINGS_H

#include "global/layer_data_types.h"

class Calculation_Settings : public QWidget
{
	Q_OBJECT
public:
	explicit Calculation_Settings(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // CALCULATION_SETTINGS_H
